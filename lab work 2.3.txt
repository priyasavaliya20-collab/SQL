QUESTION :- 1

CREATE TABLE employees(
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    position VARCHAR (50),
    salary DECIMAL (10,2)
);

INSERT INTO employees (emp_id, emp_name, position, salary) VALUES
(1, 'Alice Smith', 'Manager', 75000.00),
(2, 'Bob Johnson', 'Analyst', 60000.00),
(3, 'Charlie Brown', 'Sales Rep', 50000.00),
(4, 'Diana Prince', 'Developer', 80000.00),
(5, 'Ethan Hunt', 'Developer', 80000.00);

INSERT INTO employees (emp_id, emp_name, position, salary) VALUES
(1, 'Frank White', 'Intern', 30000.00);

QUESTION :- 2

CREATE TABLE departments (
    dept_id INT,
    dept_name VARCHAR(50)
);

ALTER TABLE departments ADD PRIMARY KEY (dept_id);

QUESTION :- 3

CREATE TABLE project_assignments (
    emp_id INT,
    project_id INT,
    assigned_date DATE,
    PRIMARY KEY (emp_id, project_id)
);

INSERT INTO project_assignments (emp_id, project_id, assigned_date) VALUES (1, 101, '2025-10-26');
INSERT INTO project_assignments (emp_id, project_id, assigned_date) VALUES (1, 102, '2025-11-01');
INSERT INTO project_assignments (emp_id, project_id, assigned_date) VALUES (2, 101, '2025-11-05');

QUESTION :- 4

CREATE TABLE student_enrollments (
    student_id INT,
    course_id INT,
    enrollment_date DATE,
    PRIMARY KEY (student_id, course_id)
);

INSERT INTO student_enrollments (student_id, course_id, enrollment_date) VALUES (1, 101, '2025-01-15');
INSERT INTO student_enrollments (student_id, course_id, enrollment_date) VALUES (1, 102, '2025-02-20');
INSERT INTO student_enrollments (student_id, course_id, enrollment_date) VALUES (2, 101, '2025-01-18');

INSERT INTO student_enrollments (student_id, course_id, enrollment_date) VALUES (1, 101, '2025-03-10');

QUESTION :- 5

CREATE TABLE departments (
    dept_id INT PRIMARY KEY,
    dept_name VARCHAR(50)
);

INSERT INTO departments (dept_id, dept_name) VALUES
(1, 'HR'),
(2, 'IT'),
(3, 'Sales');

CREATE TABLE employees (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    dept_id INT,
    FOREIGN KEY (dept_id) REFERENCES departments(dept_id)
);

INSERT INTO employees (emp_id, emp_name, dept_id) VALUES
(101, 'Amit', 1),
(102, 'Riya', 2),
(103, 'Karan', 3),
(104, 'Neha', 1),
(105, 'Rahul', 2);


QUESTION :- 6

CREATE TABLE projects (
    project_id INT PRIMARY KEY,
    project_name VARCHAR(50),
    dept_id INT,
    FOREIGN KEY (dept_id) REFERENCES departments(dept_id)
);

INSERT INTO projects VALUES (201, 'Payroll System', 1);
INSERT INTO projects VALUES (202, 'Website Development', 2);

QUESTION :- 7

CREATE TABLE employees (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    dept_id INT,
    FOREIGN KEY (dept_id) REFERENCES departments(dept_id)
    ON DELETE CASCADE
);

3️⃣ Test karo (Delete parent record)
DELETE FROM departments
WHERE dept_id = 2;

QUESTION :- 8

CREATE TABLE tasks (
    task_id INT PRIMARY KEY,
    task_name VARCHAR(50),
    project_id INT,
    FOREIGN KEY (project_id) REFERENCES projects(project_id)
);

INSERT INTO tasks (task_id, task_name, project_id) VALUES (1, 'Design Database Schema', 1);
INSERT INTO tasks (task_id, task_name, project_id) VALUES (2, 'Develop API Endpoints', 1);

SELECT
    t.task_name,
    p.project_name,
    d.dept_name
FROM tasks t
JOIN projects p
ON t.project_id = p.project_id
JOIN departments d
ON p.dept_id = d.dept_id;

QUESTION :- 9

CREATE DATABASE company_db;
USE company_db;

CREATE TABLE departments (
    dept_id INT PRIMARY KEY,
    dept_name VARCHAR(50)
);

INSERT INTO departments(dept_id,dept_name) VALUES
(1, 'HR'),
(2, 'IT'),
(3, 'Finance');


CREATE TABLE employees (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    dept_id INT,
    FOREIGN KEY (dept_id) REFERENCES departments(dept_id)
);

INSERT INTO employees(emp_id,emp_name,dept_id) VALUES
(101, 'Amit', 2),
(102, 'Neha', 1),
(103, 'Rahul', 3);


CREATE TABLE projects (
    project_id INT PRIMARY KEY,
    project_name VARCHAR(50),
    dept_id INT,
    FOREIGN KEY (dept_id) REFERENCES departments(dept_id)
);

INSERT INTO projects (project_id, project_name,dept_id)VALUES
(201, 'Website', 2),
(202, 'Payroll', 3),
(203, 'Recruitment', 1);



CREATE TABLE project_assignments (
    emp_id INT,
    project_id INT,
    PRIMARY KEY (emp_id, project_id),
    FOREIGN KEY (emp_id) REFERENCES employees(emp_id),
    FOREIGN KEY (project_id) REFERENCES projects(project_id)
);

INSERT INTO project_assignments (emp_id,project_id) VALUES
(101, 201),
(102, 203),
(103, 202);
